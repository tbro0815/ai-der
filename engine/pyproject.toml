[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-der"
dynamic = ["version"]
description = "AI-DER: SSD/RAM/CUDA tiered inference for Qwen3.8-Flash-Next"
license = "Apache-2.0"
requires-python = ">=3.12"
authors = [
    {name = "JustVugg"},
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Science/Research",
    "Operating System :: POSIX :: Linux",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

[project.optional-dependencies]
convert = [
    "numpy",
    "huggingface_hub",
]
oracle = [
    "torch>=2.0",
    "transformers>=4.40",
    "safetensors",
]
bench = [
    "tokenizers",
    "datasets",
]

[project.scripts]
ai-der = "colibri.cli:main"
coli = "colibri.cli:main"

[project.urls]
Homepage = "https://github.com/tbro0815/ai-der"
Issues = "https://github.com/tbro0815/ai-der/issues"

[tool.setuptools.dynamic]
version = {attr = "colibri._version.__version__"}

[tool.setuptools.packages.find]
where = ["."]
include = ["colibri*"]
